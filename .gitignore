result
result-*
*.tmp
*.bak

# Local overrides — user secrets and home-manager config.
# Note: my-agents.nix is intentionally NOT ignored — the flake must evaluate it,
# and flakes only see git-tracked files (run `git add my-agents.nix`).
home.nix
secrets/*.env
!secrets/hermes.env.example

# Editor artifacts
*.swp
*.swo
*~

# Nix
.direnv/
.envrc
.planning

# Reports
.reports/
# Private keys — never commit unencrypted credentials
# .age files (encrypted by agenix) ARE safe to commit — they go in git
